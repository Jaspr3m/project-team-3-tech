<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Meet</title>
    <link rel="stylesheet" href="/static/css/edit-meet.css" />
    <link rel="stylesheet" href="/static/css/header.css" />
    <link rel="stylesheet" href="/static/css/footer.css" />
  </head>
  <body>
    <header>
      <%- include('partials/header') %>
    </header>
    <main>
      <div class="meet-grid">
        <% if (typeof error !== 'undefined' && error) { %>
        <div style="color: red; margin-bottom: 1rem; font-weight: bold">
          <%= error %>
        </div>
        <% } %>
        <form
          method="POST"
          action="/edit-meet/<%= meet._id %>"
          enctype="multipart/form-data"
        >
          <label for="title">Title</label>
          <input type="text" id="title" name="title" value="<%= meet.title %>" required />

          <label for="description">Short Description:</label>
          <input
            type="text"
            id="description"
            name="description"
            maxlength="256"
            placeholder="Add a short description (max 256 characters)"
            value="<%= meet.description %>"
            required
          />

          <label for="address">Address</label>
          <input type="text" id="address" name="address" value="<%= meet.address || '' %>" required />

          <label for="date">Date:</label>
          <input type="date" id="date" name="date" value="<%= meet.date %>" required />

          <label for="time">Time:</label>
          <input type="time" id="time" name="time" value="<%= meet.time %>" required />

          <label for="maxPeople">Amount of people:</label>
          <input
            type="number"
            id="maxPeople"
            name="maxPeople"
            min="1"
            value="<%= meet.maxPeople %>"
            required
          />

          <label for="location">Location:</label>
          <select id="location" name="location" required>
            <option value="">Select a city</option>
            <option value="Amsterdam" <%= meet.location === 'Amsterdam' ? 'selected' : '' %>>Amsterdam</option>
            <option value="Rotterdam" <%= meet.location === 'Rotterdam' ? 'selected' : '' %>>Rotterdam</option>
            <option value="Utrecht" <%= meet.location === 'Utrecht' ? 'selected' : '' %>>Utrecht</option>
            <option value="Den Haag" <%= meet.location === 'Den Haag' ? 'selected' : '' %>>Den Haag</option>
          </select>

          <label for="category">Category:</label>
          <select id="category" name="category" required>
            <option value="">Select a category</option>
            <option value="Party" <%= meet.category === 'Party' ? 'selected' : '' %>>Party</option>
            <option value="Study" <%= meet.category === 'Study' ? 'selected' : '' %>>Study</option>
            <option value="Walk" <%= meet.category === 'Walk' ? 'selected' : '' %>>Walk</option>
            <option value="Food" <%= meet.category === 'Food' ? 'selected' : '' %>>Food</option>
          </select>

          <label for="image">Cover Image:</label>
          <input
            type="file"
            id="image"
            name="image"
            accept="image/*"
          />
          <% if (meet.image) { %>
            <div style="margin-bottom: 12px;">
              <img src="<%= meet.image %>" alt="Current Cover" style="max-width: 100%; max-height: 180px; border-radius: 8px;" />
              <div style="font-size: 13px; color: #666;">Current cover image</div>
            </div>
          <% } %>

          <label for="ageMin">Preferred Age Range:</label>
          <div class="side-by-side">
            <input
              type="number"
              id="ageMin"
              name="ageMin"
              min="18"
              max="99"
              value="<%= meet.ageMin || 18 %>"
              placeholder="Min Age"
              required
            />
            <input
              type="number"
              id="ageMax"
              name="ageMax"
              min="18"
              max="99"
              value="<%= meet.ageMax || 99 %>"
              placeholder="Max Age"
              required
            />
          </div>

          <div class="meet-actions">
            <button
              type="button"
              class="cancel-btn"
              onclick="window.history.back()"
            >
              Cancel
            </button>
            <button type="submit" class="create-btn">Save Changes</button>
          </div>
        </form>
      </div>
    </main>
    <footer> <%- include('partials/footer', { userId: userId }) %></footer>
  </body>
</html>
